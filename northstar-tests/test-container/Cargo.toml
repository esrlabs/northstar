[package]
name = "test-container"
version = "0.1.0"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.57"
caps = "0.5.3"
clap = { version = "3.1.18", features = ["derive"] }
nix = { version = "0.24.1", default-features = false, features = ["process", "user"] }
