[package]
name = "test-container"
version = "0.0.1"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.58"
caps = "0.5.3"
clap = { version = "3.2.12", features = ["derive"] }
nix = { version = "0.24.2", default-features = false, features = ["process", "user"] }
