[package]
name = "test-container"
version = "0.0.1"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = { version = "1.0.64", features = ["backtrace"] }
caps = "0.5.4"
clap = { version = "3.2.20", features = ["derive"] }
nix = { version = "0.25.0", default-features = false, features = ["process", "user"] }
