[package]
name = "console"
version = "0.1.0"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.57"
tokio = { version = "1.17.0", features = ["macros", "rt", "net"] }
northstar = { path = "../../northstar", features = ["api"] }
