[package]
name = "console"
version = "0.1.0"
authors = ["ESRLabs"]
edition = "2021"

[dependencies]
anyhow = "1.0.44"
tokio = { version = "1.12.0", features = ["macros", "rt", "net"] }
northstar = { path = "../../northstar", features = ["api"] }
