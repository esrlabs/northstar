[package]
name = "console"
version = "0.1.0"
authors = ["ESRLabs"]
edition = "2018"

[dependencies]
anyhow = "1.0.44"
tokio = { version = "1.12.0", features = ["macros", "rt", "net"] }
northstar = { path = "../../northstar", features = ["api"]}
futures = "0.3.17"

