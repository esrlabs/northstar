[package]
name = "token-server"
version = "0.1.0"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.56"
tokio = { version = "1.18.0", features = ["macros", "rt", "net"] }
northstar = { path = "../../northstar", features = ["api"] }
hex = "0.4.3"
