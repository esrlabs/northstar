[package]
name = "token-server"
version = "0.0.1"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.58"
hex = "0.4.3"
northstar-client = { path = "../../northstar-client" }
tokio = { version = "1.20.0", features = ["macros", "rt", "net"] }
