[package]
name = "token-server"
version = "0.0.1"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
anyhow = { version = "1.0.66", features = ["backtrace"] }
base64 = "0.13.1"
northstar-client = { path = "../../northstar-client" }
tokio = { version = "1.21.2", features = ["macros", "rt", "net"] }
