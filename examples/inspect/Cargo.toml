[package]
name = "inspect"
version = "0.0.1"
authors = ["ESRLabs"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
caps = "0.5.5"
nix = { version = "0.26.1", default-features = false, features = [ "process", "user", "signal" ] }
