name: capabilities
version: 0.0.1
init: /capabilities
capabilities:
  - cap_net_raw
suppl_groups:
  - inet
mounts:
    /lib:
      host: /lib
    /lib64:
      host: /lib64
    /system:
      host: /system
