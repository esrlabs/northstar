name: capabilities
version: 0.0.1
init: /capabilities
uid: 1000
gid: 1000
capabilities:
  - cap_net_raw
suppl_groups:
  - inet
mounts:
    /lib:
      host: /lib
    /lib64:
      host: /lib64
    /system:
      host: /system
io:
  stdout:
    log:
      - DEBUG
      - hello
