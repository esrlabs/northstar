name: cpueater
version: 0.0.1
init: /cpueater
env:
  THREADS: 4
cgroups:
  cpu:
    shares: 100
mounts:
    - source: /lib
      target: /lib
      type: bind
    - source: /lib64
      target: /lib64
      type: bind
    - source: /system
      target: /system
      type: bind
